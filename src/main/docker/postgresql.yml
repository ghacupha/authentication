version: '2'
services:
  authentication-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/authentication/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=authentication
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
